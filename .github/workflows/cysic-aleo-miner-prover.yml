name: Build and push `cysic-aleo-miner-prover` docker image

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - cysic/aleo-miner/prover/**
      - .github/workflows/cysic-aleo-miner-prover.yml
  pull_request:
    branches:
      - main
    paths:
      - cysic/aleo-miner/prover/**

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - name: Set up qemu
        uses: docker/setup-qemu-action@v3

      - name: Set up docker buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to docker hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push docker image
        uses: docker/build-push-action@v6
        with:
          file: cysic/aleo-miner/prover/Dockerfile
          platforms: linux/amd64,linux/arm64
          context: cysic/aleo-miner/prover
          push: ${{ github.ref == 'refs/heads/main' }}
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/cysic-aleo-miner-prover:latest
            ${{ secrets.DOCKER_USERNAME }}/cysic-aleo-miner-prover:v1
